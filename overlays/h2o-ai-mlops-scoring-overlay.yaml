# authorship: generated by API Evangelist tooling. Stamped 2026-08-18
# on the file's own generator header (roadmap#64). An unmarked file is
# NOT assumed to be ours -- absence of evidence was never stamped.
method: generated
overlay: 1.0.0
info:
  title: API Evangelist enhancements for the H2O MLOps Scoring REST API
  version: 1.0.0
extends: openapi/h2o-ai-mlops-scoring-openapi-original.yml
x-generated: '2026-08-04'
x-method: generated
x-source: >-
  API Evangelist enrichment pipeline. The harvested specification at
  openapi/h2o-ai-mlops-scoring-openapi-original.yml is byte-identical to the YAML H2O.ai
  links from https://docs.h2o.ai/mlops/model-scoring/mlops-scoring-rest-api.
actions:
- target: $.info
  update:
    x-apievangelist-provider: h2o-ai
    x-apievangelist-source: https://docs.h2o.ai/mlops/model-scoring/mlops-scoring-rest-api
    x-apievangelist-harvested: '2026-08-04'
    x-apievangelist-operations: 7
    x-apievangelist-docs: https://docs.h2o.ai/mlops/
    x-apievangelist-python-client: https://pypi.org/project/h2o-mlops/
    x-apievangelist-release-notes: https://docs.h2o.ai/mlops/release-notes
- target: $.info
  update:
    x-apievangelist-gaps:
      server_placeholder: >-
        The published servers[] entry is https://{defaultHost} with the variable defaulting to
        www.example.com. The real host is per-deployment, assigned by H2O MLOps when a model is
        deployed, so no callable production URL can be derived from the specification alone.
      security: >-
        components.securitySchemes is absent even though every operation declares a 401
        response, so the specification does not describe how a caller authenticates.
      tags: No operation carries a tag, and no top-level tags[] block is declared.
      operation_ids: >-
        operationIds are FastAPI autogenerated (e.g. score_rows_model_score_post) rather than
        curated, which makes generated SDK and MCP tool names awkward.
- target: $.info
  update:
    x-apievangelist-capability: >-
      Runtime scoring surface for a deployed model — identity, input/output schema, sample
      request, capability list, row scoring, media scoring, and Shapley contribution values.
